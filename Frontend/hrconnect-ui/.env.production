# API base URL used by src/api/*. In dev this stays as `/api` so the Vite
# proxy (see vite.config.ts) forwards calls to the backend over HTTPS.
# In production point this at the deployed API origin, e.g. https://api.example.com
VITE_API_BASE_URL=https://hrconnect-api-priya-buccf7fwbmgnfecu.centralindia-01.azurewebsites.net/api
